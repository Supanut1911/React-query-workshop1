import Layout from '@/components/layout/Layout';
import PostView from '@/components/posts/posts';

const Post = () => {
  return (
    <Layout>
      <PostView />
    </Layout>
  );
};

export default Post;
